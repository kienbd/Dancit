<% if @res %>
  $(".follow-btn").html("<%=j link_to 'unfollow',unfollow_user_path(follow_id: params[:follow_id]),method: :delete, remote: true,class: 'btn btn-danger btn-block' %>")
<% else %>
  alert("Failed");
<% end %>
