<% if @res %>
  $(".follow-btn").html("<%=j link_to 'follow',follow_user_path(follow_id: params[:follow_id]),method: :post, remote: true,class: 'btn btn-default btn-block' %>")
<% else %>
  alert("Failed");
<% end %>
